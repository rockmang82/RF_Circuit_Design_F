# Circuit_SmithChart.py — 완전 구현 명세서
# 대상: Claude Code Sonnet 4.6 | 단일 .py 파일 | 한국어 주석

---

## 1. 프로젝트 개요

수동 회로 소자(R, L, C)를 GUI에서 배치·연결하여 1-Port Network를 구성하고,
MNA(Modified Nodal Analysis) 행렬 기반으로 주파수 Sweep 임피던스를 계산하여
Smith Chart 및 Magnitude/Phase 그래프에 출력하는 **단일 파이썬 파일** 애플리케이션.

### 기술 스택
- **프레임워크**: PyQt5
- **그래프**: matplotlib (FigureCanvasQTAgg 임베딩)
- **수치 계산**: numpy
- **구조**: 단일 `Circuit_SmithChart.py` 파일
- **주석**: 한국어로 작성
- **라이브러리 제약**: 없음 (필요 시 자유롭게 import 가능)
- **구현 자유도**: 본 명세에 명시되지 않은 세부 구현(클래스 구조, 내부 변수명, 헬퍼 함수 등)은 개발자가 최적이라 판단하는 방식으로 자유롭게 결정

---

## 2. 메인 윈도우 레이아웃

아래 ASCII 다이어그램과 **첨부된 참조 이미지(Image 1)**를 함께 참조하여 구현한다.

```
┌─────────────────────────────────────────────────────────────────────┐
│  RF Circuit Design - Smith Chart Analyzer                [─][□][X] │
├─────────────────────────────────────────────────────────────────────┤
│ [R] [L] [C]   시작(MHz):[____]  끝(MHz):[____]  [Cal]             │
│                       [Sensitivity Analysis]            [Advanced]  │
├──────────────────────────────────┬──────────────────────────────────┤
│                                  │                                  │
│       창1: 회로 설계 영역          │       창2: Smith Chart 영역       │
│       (CircuitCanvas)            │       (SmithChartWidget)         │
│                                  │                                  │
│  Port ●──┬── R(50Ω) ── L(20nH)──┤          ◯ Smith Chart           │
│     +    │                  │    │                                  │
│          │             C(30pF)   │       ■ 시작마커  ■ 끝마커         │
│          │                  │    │                                  │
│  Port ●──┴──────────────────┘    │                                  │
│    GND                           │                                  │
│                                  │                                  │
├──────────────────────────────────┴──────────────────────────────────┤
│ 상태바                                                               │
└─────────────────────────────────────────────────────────────────────┘
```

### 2-1. 윈도우 사양

| 항목 | 값 |
|------|-----|
| 기본 크기 | 1600 × 800 px |
| 최소 크기 | 1200 × 600 px (`setMinimumSize`) |
| 리사이즈 | 자유 리사이즈 가능 |
| 창1:창2 비율 | 60:40 (`QSplitter(Qt.Horizontal)`) |
| 비율 동작 | 윈도우 리사이즈 시 현재 비율 자동 유지. 사용자가 QSplitter 핸들 드래그로 수동 변경 가능 |

### 2-2. 상단 툴바 영역

| 요소 | 설명 |
|------|------|
| `[R]` `[L]` `[C]` 버튼 | 클릭 시 해당 소자의 배치 모드 진입 |
| `시작(MHz)` / `끝(MHz)` | QLineEdit, QDoubleValidator로 숫자만 허용 |
| `[Cal]` 버튼 | MNA 임피던스 계산 실행 (QThread 백그라운드) |
| `[Sensitivity Analysis]` 버튼 | 배치만 함. 클릭 시 `QMessageBox.information("추후 구현 예정")` 표시. 스타일: 배경색 `#4A90D9`, 흰색 텍스트. 위치: 상단 중앙 (Image 1 참조) |
| `[Advanced]` 버튼 | 창2 우측 상단 배치. 클릭 시 윈도우2(Advanced Window) 열기. 80×28px, 배경색 `#4A90D9`, 흰색 텍스트, 10pt |

---

## 3. 창1 — 회로 설계 영역 (CircuitCanvas)

`QWidget`을 상속하여 `paintEvent`에서 QPainter로 전체를 재드로잉한다.

### 3-1. 그리드 시스템

| 항목 | 값 |
|------|-----|
| **스냅 그리드** | **10px** 단위 (소자 위치, 와이어 경유점 모두 10px에 정렬) |
| **시각 그리드** | **20px** 간격 점선, 색상 `#BDBDBD`, `Qt.DotLine` |

### 3-2. Port (고정 요소)

- 창1 좌측에 고정 배치 (사용자가 이동·삭제 불가)
- 두 개의 연결점: **상단 핀(+, 신호선)** / **하단 핀(GND)**
- 심볼 렌더링:
  - 세로선: `x=80`, `y_plus ~ y_gnd` 구간
  - 상단 핀(●): 원 반지름 5px, 채움 `#4CAF50`, 옆에 "+" 레이블
  - 하단 핀(●): 원 반지름 5px, 채움 `#4CAF50`, 옆에 "GND" 레이블
  - "Port" 텍스트: 세로선 좌측 10px 위치

### 3-3. 소자 심볼 렌더링 (QPainterPath 기반, 수평 기준)

모든 소자는 중심점 `(x, y)` 기준으로 렌더링. 총 폭 60px (핀 포함), 선 색상 `#212121`, 두께 2px.

#### R (저항) — 지그재그
```
좌측 핀:  (x-30, y) → (x-20, y) 직선
지그재그: (x-20,y) → (x-15,y-8) → (x-5,y+8) → (x+5,y-8) → (x+15,y+8) → (x+20,y)
우측 핀:  (x+20, y) → (x+30, y) 직선
```

#### L (인덕터) — 반원 코일
```
좌측 핀:  (x-30, y) → (x-20, y) 직선
코일부:   4개의 반원 아크, 반지름 5px, 위쪽 볼록 (x-20 ~ x+20 구간, 각 10px 폭)
우측 핀:  (x+20, y) → (x+30, y) 직선
```

#### C (커패시터) — 평행 평판
```
좌측 핀:  (x-30, y) → (x-4, y) 직선
좌측 판:  x=-4 위치, y-12 ~ y+12 수직선, 두께 3px
우측 판:  x=+4 위치, y-12 ~ y+12 수직선, 두께 3px
우측 핀:  (x+4, y) → (x+30, y) 직선
```

#### 수직(90°) 소자
수평 심볼의 좌표 변환으로 렌더링 (x↔y 교환, 부호 조정). 핀이 상·하로 나옴. 구체적 변환 방식은 자유.

### 3-4. 소자 상태별 색상

| 상태 | 스타일 |
|------|--------|
| 기본 | 검정 `#212121`, 두께 2px |
| 선택됨 | 파란 외곽선 `#1565C0`, 두께 3px |
| 에러 (error_highlight) | 빨간 외곽선 `#D32F2F`, 두께 3px, 배경 `#FFEBEE` |

### 3-5. 소자 값 레이블

| 항목 | 값 |
|------|-----|
| 수평 소자 위치 | 소자 중심 **아래** 10px |
| 수직 소자 위치 | 소자 중심 **우측** 14px |
| 표시 형식 | `50Ω`, `10nH`, `3.3pF` |
| 미입력 시 | `?` (색상 `#9E9E9E`) |
| 폰트 | 9pt, Arial |

---

## 4. 소자 인터랙션 시스템

### 4-1. 새 소자 배치 (고스트 프리뷰 방식)

```
동작 흐름:
1. 상단 패널의 [R]/[L]/[C] 버튼 클릭 → 배치 모드 진입
2. 마우스가 창1 위를 이동 → 반투명 고스트 이미지가 마우스 커서를 따라 이동 (10px 스냅 그리드에 정렬)
3. 창1 위에서 클릭 → 해당 위치에 소자 배치 확정, 배치 모드 자동 해제 (1회 배치)
4. ESC 키 → 배치 모드 취소
```

고스트 이미지 스타일:
- 투명도 50%: `QPainter.setOpacity(0.5)`
- 10px 스냅 그리드에 정렬하여 표시
- 연결선 미리보기: 회색 `#AAAAAA`, `Qt.DashLine` 점선

### 4-2. 기존 소자 이동 (클릭+드래그 + 고스트 프리뷰)

```
동작 흐름:
1. 소자를 클릭 → 선택 상태 (파란 외곽선)
2. 선택된 소자를 다시 클릭한 채로 드래그 시작 → 고스트 이미지가 마우스를 따라 이동
3. 드래그 중: 고스트는 10px 스냅 그리드에 정렬, 기존 연결선의 변경 미리보기 (회색 점선)
4. 마우스 릴리즈 → 해당 위치에 소자 이동 확정, 연결된 와이어 자동 재라우팅
5. ESC 또는 캔버스 밖으로 드래그 → 이동 취소, 원래 위치로 복귀
```

### 4-3. 소자 회전

- 소자를 클릭하여 선택 후 **R 키** → 90° 시계방향 회전
- 수평 ↔ 수직 토글
- 회전 시 연결된 와이어 자동 재라우팅

### 4-4. 소자 값 입력

- 소자를 **더블 클릭** → `QInputDialog`로 값 입력
- 단위 체계:
  - R: Ω (예: `50` 입력 → `50Ω` 표시)
  - L: nH (예: `10` 입력 → `10nH` 표시)
  - C: pF (예: `3.3` 입력 → `3.3pF` 표시)

### 4-5. 소자 삭제

- 소자를 클릭하여 선택 후 **Delete 키** → 소자 삭제
- 해당 소자에 연결된 모든 와이어도 함께 삭제

---

## 5. 와이어링 (연결) 시스템

### 5-1. 연결 방식: 핀-핀 클릭 방식

```
동작 흐름:
1. 소자의 핀(리드선 끝점) 또는 Port 핀을 클릭 → 연결 시작점 선택 (핀 하이라이트)
2. 다른 소자의 핀 또는 Port 핀을 클릭 → 연결 완료
3. 두 핀 사이에 L자형 직각 와이어가 자동 생성
```

### 5-2. L자형 직각 자동 라우팅

- 두 핀 사이를 **L자형 직각 꺾임선**으로 자동 연결
- 경유점은 10px 스냅 그리드에 정렬
- 라우팅 방향 결정: 수평 먼저 → 수직 (또는 반대) — 핀의 방향(수평 소자의 좌/우, 수직 소자의 상/하)에 따라 자연스러운 방향을 자동 선택
- 소자 이동·회전 시 연결된 와이어 자동 재라우팅

### 5-3. 와이어 시각적 표현

| 상태 | 색상 | 두께 |
|------|------|------|
| **연결 완료** (양 끝이 핀에 연결됨) | 빨간색 `#F44336` | 2px |
| **미연결** (한쪽만 연결 또는 드래그 중) | 검정색 `#212121` | 2px |

### 5-4. 연결 규칙

- **핀과 핀 사이만** 연결 가능 (소자 본체 클릭은 연결 불가)
- 하나의 핀에 **여러 와이어** 연결 가능 (분기점/노드 형성)
- Port 상단 핀 = 신호선(+), Port 하단 핀 = GND

---

## 6. 임피던스 계산 엔진 (MNA 기반)

### 6-1. 계산 방식: Modified Nodal Analysis (MNA) 행렬

직렬/병렬 재귀 축약이 **아닌**, 행렬 기반 MNA를 사용하여 **임의의 회로 토폴로지**(브리지 회로 등 포함)를 지원한다.

```
MNA 행렬 구성 흐름:
1. 회로망의 노드를 식별 (핀 연결 관계에서 동일 전위 노드 그룹화)
2. GND 핀이 속한 노드를 기준 노드(0번)로 지정
3. 각 주파수 f에 대해:
   a. 노드 어드미턴스 행렬 Y(f) 구성
      - R: Y = 1/R (주파수 무관)
      - L: Y = 1/(j·2π·f·L),  L은 nH→H 변환 (×10⁻⁹)
      - C: Y = j·2π·f·C,      C는 pF→F 변환 (×10⁻¹²)
   b. Port(+) 노드에 1A 전류원 인가
   c. Y·V = I 연립방정식 풀기 → V_port(+) 전압 산출
   d. Z(f) = V_port(+) / 1A = V_port(+)
4. 결과: 각 주파수별 Z(f) = R(f) + jX(f)
```

### 6-2. 주파수 설정

| 항목 | 값 |
|------|-----|
| 입력 방식 | 상단 툴바의 `시작(MHz)` / `끝(MHz)` 필드에 수동 입력 |
| 단위 | MHz |
| 주파수 스텝 | **0.01 MHz** 고정 |
| 예시 | 100 ~ 200 MHz → 100.00, 100.01, 100.02, ..., 200.00 (총 10,001개 포인트) |

### 6-3. 계산 실행 및 스레딩

- **[Cal] 버튼** 클릭 시 계산 시작
- **QThread** 백그라운드 처리로 UI 프리징 방지
- 계산 진행 중 **프로그레스바** 표시 (QProgressBar 또는 QProgressDialog)
- 계산 완료 시 시그널로 메인 스레드에 결과 전달 → Smith Chart 및 Advanced 그래프 업데이트

### 6-4. 에러 검증 (계산 전)

[Cal] 버튼 클릭 시 아래 조건을 **순서대로** 검증하고, 위반 시 `QMessageBox.warning()`으로 경고 후 계산 중단:

1. 주파수 값이 비어있거나 숫자가 아닌 경우
2. 시작 주파수 ≥ 끝 주파수인 경우
3. Port에 아무 소자도 연결되지 않은 경우
4. 연결된 소자 중 값이 입력되지 않은 소자가 있는 경우 → 해당 소자를 `error_highlight=True`로 표시
5. 미연결 핀이 있는 소자가 존재하는 경우 (모든 핀이 네트워크에 속해야 함) → 해당 소자를 `error_highlight=True`로 표시

---

## 7. Smith Chart 출력 (창2)

### 7-1. Smith Chart 렌더링 (matplotlib 임베딩)

- `FigureCanvasQTAgg`로 창2에 임베딩
- 기준 임피던스: **Z₀ = 50Ω**

배경 요소:
- **등저항 원** (Constant Resistance Circles): r = 0, 0.2, 0.5, 1, 2, 5
- **등리액턴스 호** (Constant Reactance Arcs): x = ±0.2, ±0.5, ±1, ±2, ±5
- **실수 축**: 수평 중심선
- **단위원**: |Γ| = 1
- 등저항/등리액턴스 선 색상: 연한 회색, 단위원: 검정

### 7-2. 계산 결과 플로팅

- **반사계수**: `Γ(f) = (Z(f) - Z₀) / (Z(f) + Z₀)`
- 플로팅: `Γ`의 실수부(x축) vs 허수부(y축)
- 주파수별 점들을 연결하여 하나의 연속 라인 형성
- 라인 색상: 파란색 `#1E88E5`, 두께 2pt

### 7-3. 마커 기능

| 마커 | 심볼 | 위치 |
|------|------|------|
| 시작 주파수 | ■ (정사각형, 파란색) | 시작 주파수의 Γ 위치 + 주파수 레이블 (예: "100MHz") |
| 끝 주파수 | ■ (정사각형, 빨간색) | 끝 주파수의 Γ 위치 + 주파수 레이블 (예: "1MHz") |
| 사용자 마커 | ● (원형) | Smith Chart 라인 위 클릭 시 가장 가까운 주파수 포인트에 배치 |

사용자 마커 클릭 시 표시 정보:
```
f = {freq}MHz, Z = {R} + j{X} Ω, Γ = {real} + j{imag}
```
재클릭 시 기존 마커 제거 후 새 마커 표시.

참조 이미지(Image 1)의 Smith Chart 영역에서 시작/끝 마커의 시각적 스타일을 확인할 수 있다.

---

## 8. Advanced 윈도우 (윈도우2)

**[Advanced] 버튼** 클릭 시 열리는 독립 윈도우. **참조 이미지(Image 2)**를 함께 참조하여 구현한다.

### 8-1. 윈도우 사양

| 항목 | 값 |
|------|-----|
| 타입 | `QDialog` (비모달 — 메인 윈도우와 동시 조작 가능) |
| 기본 크기 | 800 × 600 px |
| 최소 크기 | 600 × 400 px |
| 타이틀 | `"Advanced - Impedance Analysis"` |
| 중복 방지 | 이미 열려있으면 포커스 이동, 중복 생성하지 않음 |
| 닫기 | 표준 X 버튼, 다시 Advanced 클릭 시 재생성하여 마지막 계산 결과 즉시 표시 |

### 8-2. 윈도우2 내부 레이아웃

```
┌──────────────────────────────────────────────────────┐
│  Advanced - Impedance Analysis              [?][X]   │
├──────────────────────────────────────────────────────┤
│  Scale: [Linear ▼]        Mag Unit: [Ω ▼]           │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │            그래프3 (상단 50%)                   │    │
│  │  Y축: Magnitude (Ω)     라인: #1E88E5 파란색   │    │
│  │  X축: Frequency (MHz)                        │    │
│  │              ┃ ← 커서라인 (수직, 동기화)        │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │            그래프4 (하단 50%)                   │    │
│  │  Y축: Phase (°)         라인: #E53935 빨간색   │    │
│  │  X축: Frequency (MHz)                        │    │
│  │              ┃ ← 커서라인 (수직, 동기화)        │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
├──────────────────────────────────────────────────────┤
│  Cursor: f=150.00MHz  |Z|=73.2Ω  ∠Z=+45.3°  Γ=... │
└──────────────────────────────────────────────────────┘
```

### 8-3. 그래프3 — 주파수 vs Magnitude

#### X축 (주파수)
- 범위: 메인 윈도우의 Start/Stop 주파수 자동 사용
- 스케일: 기본 Linear, 툴바 Scale 드롭다운으로 Logarithmic 전환 가능
- 라벨: `"Frequency (MHz)"`
- 눈금: `matplotlib AutoLocator`

#### Y축 (Magnitude)
- 기본 단위: **Ω** (절대 임피던스 크기 `|Z| = sqrt(R² + X²)`)
- 대체 단위: **dB** (`20 * log10(|Z|)`)
- 전환: 툴바 Mag Unit 드롭다운 `Ω ↔ dB`
- 라벨: `"Magnitude (Ω)"` 또는 `"Magnitude (dB)"` 동적 변경
- 눈금: 자동 스케일

#### 플롯 스타일
- 라인 색상: `#1E88E5` (파란색), 두께 1.5pt
- 배경: 흰색, 격자 on (색상 `#E0E0E0`, 점선)

### 8-4. 그래프4 — 주파수 vs Phase

#### X축 (주파수)
- 그래프3과 **동일 설정 공유** (Scale 전환 시 동기화)

#### Y축 (Phase)
- 범위: **-180° ~ +180°** 고정
- 라벨: `"Phase (°)"`
- 눈금: **45° 간격** 고정 (-180, -135, -90, -45, 0, 45, 90, 135, 180)
- 계산식: `∠Z(f) = atan2(X, R) × (180/π)`

#### 플롯 스타일
- 라인 색상: `#E53935` (빨간색), 두께 1.5pt
- 배경: 흰색, 격자 on (색상 `#E0E0E0`, 점선)

### 8-5. 수직 커서라인 (Crosshair Cursor)

#### 동작
- 그래프3 또는 그래프4 위에서 **마우스 클릭** → 해당 주파수 위치에 수직 커서라인 생성
- **두 그래프 동기화**: 한 그래프에서 커서 클릭 → 다른 그래프에도 동일 주파수 위치에 커서 표시

#### 커서 스타일
- 색상: `#FF9800` (오렌지), 두께 1px, 실선
- 교차점: 커서와 데이터 라인이 만나는 점에 원형 마커 (반지름 4px, 동일 오렌지색)

#### 값 표시
**상태바** (윈도우2 하단):
```
Cursor: f={freq}MHz  |Z|={mag}{unit}  ∠Z={phase}°  Γ={gamma_real}+j{gamma_imag}
```
예시: `Cursor: f=150.00MHz  |Z|=73.2Ω  ∠Z=+45.3°  Γ=0.12+j0.34`

**그래프 위 라벨** (커서 교차점 옆):
- 그래프3: `{mag}{unit}` (예: "73.2Ω")
- 그래프4: `{phase}°` (예: "+45.3°")
- 라벨 배경: 반투명 흰색 `rgba(255,255,255,0.8)`, 폰트 9pt

### 8-6. 툴바 컨트롤

#### Scale 드롭다운 (X축 스케일)
- 위치: 윈도우2 상단 툴바 좌측
- 옵션: `"Linear"`, `"Logarithmic"`
- 기본값: `"Linear"`
- 동작: 변경 즉시 그래프3과 그래프4의 X축 동시 변경
- 구현: `ax.set_xscale('linear')` / `ax.set_xscale('log')`

#### Mag Unit 드롭다운 (Y축 단위)
- 위치: 툴바 우측
- 옵션: `"Ω"`, `"dB"`
- 기본값: `"Ω"`
- 동작: 변경 즉시 **그래프3만** Y축 데이터 및 라벨 변경 (그래프4에는 영향 없음)

---

## 9. 데이터 동기화 및 업데이트

### 9-1. 업데이트 트리거 및 데이터 흐름

```
[Cal 버튼 클릭]
       │
       ▼
[에러 검증 통과]
       │
       ▼
[QThread: MNA 임피던스 계산] → Z(f) = R(f) + jX(f) (각 주파수별)
       │
       ▼ (계산 완료 시그널)
       │
       ├──→ [창2: Smith Chart]  : Γ(f) = (Z(f)-50)/(Z(f)+50), 정규화된 임피던스 궤적
       │
       ├──→ [그래프3: Magnitude] : |Z(f)| = sqrt(R²+X²) 또는 20·log₁₀(|Z|) dB
       │
       └──→ [그래프4: Phase]     : ∠Z(f) = atan2(X, R) × (180/π), 범위 -180°~+180°
```

### 9-2. 윈도우2 상태별 동작

| 상태 | 동작 |
|------|------|
| 윈도우2 열려있는 상태에서 Cal 클릭 | Smith Chart + 그래프3/4 동시 업데이트 |
| 윈도우2 닫혀있는 상태에서 Cal 클릭 | 데이터는 내부 저장. 윈도우2 다시 열면 마지막 결과 즉시 표시 |
| 계산 미실행 상태 | 빈 그래프에 중앙 텍스트: `"No data - Press Calculate"` |
| 회로 오류 발생 시 | 그래프 상단에 빨간색 경고 메시지 텍스트 |

---

## 10. 전체 유저 워크플로우

```
1. [R]/[L]/[C] 버튼 클릭 → 배치 모드 진입 → 고스트가 마우스를 따라다님 → 클릭으로 배치 확정
2. 소자 더블 클릭 → 값 입력 (QInputDialog)
3. 핀 클릭 → 다른 핀 클릭 → L자형 와이어 자동 생성 (빨간색)
4. 필요 시: 소자 선택 후 R키로 회전, Delete키로 삭제, 클릭+드래그로 이동
5. 시작/끝 주파수 입력 → [Cal] 클릭
6. QThread 계산 중 프로그레스바 표시
7. 계산 완료 → Smith Chart 자동 업데이트
8. [Advanced] 클릭 → Magnitude/Phase 그래프 확인, 커서로 값 탐색
9. Smith Chart 위 클릭 → 주파수/임피던스/반사계수 마커 표시
```

---

## 11. 구현 지침 및 제약

| 항목 | 내용 |
|------|------|
| **파일 구조** | 단일 `Circuit_SmithChart.py` 파일 |
| **주석 언어** | 한국어 |
| **라이브러리** | 제약 없음 (PyQt5, matplotlib, numpy 필수, 기타 자유) |
| **구현 자유도** | 클래스 구조, 내부 변수명, 헬퍼 함수, 디자인 패턴 등은 개발자 판단에 따라 최적의 방식으로 자유 구현 |
| **MNA 엔진** | 반드시 행렬 기반 MNA 사용 (직렬/병렬 재귀 축약이 아님). 임의의 토폴로지(브리지 회로 등) 지원 필수 |
| **QThread** | 계산은 반드시 QThread 백그라운드로 실행, 프로그레스바 표시 |
| **참조 이미지** | Image 1(메인 윈도우), Image 2(Advanced 윈도우)의 레이아웃과 시각적 스타일을 최대한 따를 것 |
| **Sensitivity Analysis 버튼** | 버튼만 배치, 클릭 시 "추후 구현 예정" 알림. 향후 기능 추가를 고려한 구조로 배치 |

---

## 부록: 확정된 설계 결정 요약

이전 논의를 통해 확정된 모든 모호성 해소 결정을 아래에 요약한다:

| # | 항목 | 결정 |
|---|------|------|
| 1 | 그리드 크기 | 10px 스냅 / 20px 시각 점선 |
| 2 | 주파수 스텝 | 0.01 MHz |
| 3 | 메인 윈도우 크기 | 1600 × 800 px |
| 4 | 임피던스 계산 | MNA 행렬 기반 (임의 토폴로지 지원) |
| 5 | 새 소자 배치 | 고스트 프리뷰가 마우스를 따라다니다가 클릭으로 확정 |
| 6 | 기존 소자 이동 | 선택된 소자를 클릭+드래그로 이동 |
| 7 | 스레딩 | QThread 사용 + 프로그레스바 표시 |
| 8 | 와이어 라우팅 | L자형 직각 자동 라우팅 |
| 9 | 코드 구조 | 단일 .py 파일 + 한국어 주석 |
| 10 | Sensitivity Analysis | 버튼만 배치 (기능 추후 구현) |
